services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: precifly
      POSTGRES_PASSWORD: precifly
      POSTGRES_DB: precifly
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U precifly"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
